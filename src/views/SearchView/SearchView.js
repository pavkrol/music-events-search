import React from 'react';
import styles from './SearchView.module.scss';

const SearchView = () => (
  <section className={styles.wrapper}>
    <h2>This is search view</h2>
  </section>
);

export default SearchView;